description: 'Observational Data Catalog'
sources:

  ocn:
    args:
      path: "{{CATALOG_DIR}}/ocn.yaml"
    description: 'Ocean Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
    
    
  land:
    args:
      path: "{{CATALOG_DIR}}/land.yaml"
    description: 'Land/Hyrdro Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
    
    
  fluxes:
    args:
      path: "{{CATALOG_DIR}}/fluxes.yaml"
    description: 'Radiation and Atmosphere-Ocean Fluxes Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
  atm:
    args:
      path: "{{CATALOG_DIR}}/atm.yaml"
    description: 'Atmosphere Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  seaice:
    args:
      path: "{{CATALOG_DIR}}/seaice.yaml"
    description: 'Seaice Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
